--- E1 ---

sched.c = inicialización de freequeue y readyqueue, idle, init, y la parte C de inner_task_switch

task_switch.S = assembler de task_switch y una parte del inner_task_switch

user.c = se llama a fork() y demás

interrupt.c = WriteMSR
 
system.c = se inicializa todo

mm.c / list.c = operaciones de memoria y listas
